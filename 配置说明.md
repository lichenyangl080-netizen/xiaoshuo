# 小说项目技能配置

安装范围：仅本项目 `.agents/skills/`。主入口 `novel-project` 与项目 `novel-*` 专项技能允许按任务自动选择，也支持明确指定技能。自动调用不是后台运行、自动定案或批量续写。

辅助技能采用本项目命名，避免与全局通用名称混淆。日常由主技能按需读取；明确要求某项专项工作也可直接匹配对应技能。其他项目的全局技能不修改。

## 来源与适配

- 上游：https://github.com/tance-mang/chinese-webnovel-skills
- 固定版本：`ecf552f6930e769d8bbf17818ad3d5a864a7a70b`
- 改编模块：world、character、outline、memory、continuity、dialogue。
- 不长期保存整套上游源码副本；来源地址和固定版本足以供需要时重新核对。六个适配模块各自保留MIT许可证。
- 激活版本是审阅后编写的项目适配版，不声称与上游功能或默认流程完全相同。移除了固定商业节奏、强制人设标签/打脸、默认机械短句、自动Git操作及未安装技能依赖。
- 项目主技能为本项目编写。HZ-KMNO的反馈修订和人物信息边界思路供设计参考，未安装其整套技能或私人资料依赖。
- `novel-prose` 为本项目编写的统一正文精修入口。设计时核对了 `zenstory-ai/oh-story-claudecode` 的 `story-deslop`（`0ffe7db`）、`ruodou233/de-ai-taste`（`421995e`）与 `wgwtest/novel-writing`（`c57b5c6`）；只吸收小说场景诊断、误判保护和风格保真思路，没有安装这些仓库或照搬其固定流程。
- 现有全局 expand/hook/deslop 未改动，若在本项目使用须服从作者要求及项目规则。

原始作者共识本地保存于 `资料/作者原始设计共识.txt`。共享环境缺少该文件时，以 `项目档案/` 中的筛选结果为接续依据。

## 使用

直接说“继续讨论歌谣危机”“检查这个设定”“根据我的章纲写初稿”等即可。也可以指定 `$novel-project`、`$novel-world`、`$novel-character`、`$novel-outline`、`$novel-memory`、`$novel-continuity`、`$novel-dialogue`、`$novel-prose`。

官方说明：https://learn.chatgpt.com/docs/build-skills 。仓库级技能目录为 `.agents/skills`，`allow_implicit_invocation: true` 允许自动选择。新安装技能应从下一轮可用；若列表未刷新，可重新打开本项目任务或重启应用。

今后更新先对照固定版本和本地适配，不直接用上游覆盖项目规则。
