# 小说项目协作

这是作者主导、长期维护的原创小说项目。处理本项目构思、研究、设定、写作、修订或档案时，先读 `.agents/skills/novel-project/SKILL.md`，再按任务选取辅助模块。安装、排错等技术任务不必启动创作流程。

当前阶段见 `项目档案/当前进度.md`。本地原始设计保存在 `资料/作者原始设计共识.txt`；它包含确定原则、倾向、候选和举例，不是整份正式设定。`资料/` 默认不进入共享仓库；共享环境缺少原文时，以项目档案为准并如实说明证据范围，不因此中断普通接续。

接续默认只读当前进度及本次相关专题；本地原始长文按需追溯，不每轮全量加载。处理新讨论按 `项目档案/资料筛选规则.md` 筛选增量；本地终局交接保存在 `资料/终局与世界末期讨论原始交接.txt`。助手旧建议不自动成为约束。

- 作者最新明确指示优先。AI建议未经采纳不得升级成正式设定。引用原文时保留“暂时、倾向、可能”等限定。
- 第三人称限知为主；维护事实、文明理论、人物认知、读者所知的区别。
- 作者主导重要选择；明确的小任务直接完成，不机械逐步索取确认。自动选择技能不等于自动续写、定案或发布。
- 保存作者修改，避免覆盖原始资料或已确认正文。发现核心冲突说明证据与影响，请作者裁决。
- 以完整因果、人物独立性、世界一致性及阅读体验判断质量，不套固定打脸、六章节奏或短期伏笔回收公式。
- 本项目使用 `novel-*` 适配技能。全局 `expand`、`hook`、`deslop` 如被使用，同样服从作者要求；不改变全局技能安装。
- 技能的上游来源和固定版本只作维护记录，见 `配置说明.md`；上游示例、平台规则和未安装技能名称不能当作本项目事实或已具备能力。
- 安装或维护不授权推送、发布、自动提交全部文件或破坏性回滚。

## 本地 Git 自动提交

作者已授权助手在以下有意义的阶段自动进行本地提交，无须逐次确认。目的是保留可比较的创作版本，供以后回顾取舍、总结文风；不按时间定期提交，不启动后台监控。上传 GitHub 由作者负责，助手不自动推送。

### 提交时机

- 作者表示本轮“改好了”，助手准备分析或继续修改时，先检查相关文件，将尚未提交的作者修订单独保存。“改好了”仅表示一轮结束，只有作者明确确认才标为定稿。
- 助手修改正文前，检查是否存在尚未保存到 Git 的作者改动；有则先提交作者版本，再开始改稿，避免两者混在一次提交中。
- 助手完成一轮完整初稿或改稿，检查结束并准备交付时，提交本轮成果，标为“待审”。一轮内部的措辞尝试、排版修正和临时版本不逐次提交。
- 作者明确确认章节、采纳重要设定，或完成有实际作用的项目规则调整时，提交对应成果。
- 大幅调整剧情、结构或人物关系前，若现状尚未入库，先保存可恢复的基线；同一版本已提交则不重复。
- 零散错字、标点等低价值修改合并到本轮结束后提交；仅讨论而文件无变化、内容已保存或只有临时产物时不提交。每次提交应能说清独立的保存价值。

### 版本归属与说明

- 区分作者修订、助手修改、正文确认、设定确认及项目维护；助手稿被保存不代表作者已采纳。
- 提交标题采用“类型｜对象｜本轮主要变化”，例如“作者修改｜第二章｜压缩重复问答”“助手修改｜第二章｜调整对白与氛围｜待审”“正文确认｜第一章 变故”“项目维护｜自动提交规则”。
- 变化说明依据实际差异和作者明确反馈。作者未说明意图时可以写“作者修订”，不替作者推断写作原则。不仅凭文件修改时间判断作者身份；已有改动混合且无法可靠区分时，标为“现状基线｜对象｜来源混合”，不虚构历史或归属。
- 涉及从另一文件另存、改名或接着某个版本修订时，在提交说明中写明已知的底稿路径与版本；必要时注明底稿提交号，便于后续比较 Word 正文。缺少依据就如实记录，不猜测。
- Git 保存版本，文风档案保存作者明确说明的取舍理由；相关理由与本轮修改一同提交。助手的分析仍标为观察或待确认，不能升级为作者偏好。需要比较 Word 时按需提取历史正文，不为了提交额外堆积全文副本、报告或自动生成的文风结论。

### 范围与执行

- 每次提交前检查工作区、暂存区及相关差异，只暂存本轮明确涉及的文件。不得使用全仓库自动暂存；已有无关暂存内容也不能混入本次提交或被擅自撤销。
- 有比较价值的作者正文和助手完整稿可以入库，包括 `正文/草稿/` 中的有效 Word 版本。该目录目前被忽略；需要保存时可仅对核实过的具体正文文件使用 `git add -f -- <文件>`，不整体强制加入目录或全局取消忽略。草稿入库后会随作者以后上传仓库，这不等于正文定稿。
- `资料/`、研究原文及现有排除的私有资料仍不纳入；临时脚本、Word 锁文件、渲染图片、检查用 PDF、缓存和备份不提交。
- 提交前核对实际暂存范围，提交后核对成功结果与所含文件，并在交付时简短告知提交说明和短提交号。保留其他未提交改动。
- 自动提交不授权改写历史、强制推送、破坏性回滚、覆盖作者文件或擅自设置 Git 身份。若提交受权限、仓库状态或身份配置阻碍，保留工作成果并如实说明，不把未成功的提交报告为已保存。
